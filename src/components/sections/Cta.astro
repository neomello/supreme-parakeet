---
import { cta, autor } from "@data/proposta";
---

<footer class="cta glass fade-in">
  <!-- <hr class="rule" /> removido porque o cartao em si ja separa -->
  <div class="row">
    <div class="left">
      <strong>{cta.titulo}</strong>
      <span>{cta.descricao}</span>
    </div>
    <div class="right mono">
      proposta preparada por {autor.nome}<br />
      {autor.horizonte}
    </div>
  </div>
</footer>

<style>
  .cta {
    margin-top: 40px;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    box-shadow: var(--glass-shadow);
    border-radius: 16px;
    padding: 32px;
  }
  .row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 24px;
    padding-top: 26px;
  }
  .left strong {
    display: block;
    font-size: var(--fs-body);
    font-weight: 700;
    margin-bottom: 4px;
  }
  .left span {
    font-size: var(--fs-small);
    color: var(--ink-2);
  }
  .right {
    font-size: var(--fs-micro);
    color: var(--ink-faint);
    text-align: right;
    line-height: 1.7;
  }
  @media (max-width: 720px) {
    .row {
      flex-direction: column;
      align-items: flex-start;
      gap: 14px;
    }
    .right {
      text-align: left;
    }
  }
</style>
